# Proton GE Installer

A simple Bash script for installing Proton GE.

Save the `proton-ge-installer.sh` file to your computer, and in your terminal run `chmod +x proton-ge-installer.sh` to make the script executable.

To run the script, execute `./proton-ge-installer.sh`.
